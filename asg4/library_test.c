#include "library.h"

int main() {
	char *value = "value";
	
	char* key[4000];

	initialize("test", 1000, 1500);

	for(i=0; i < 2000; ++i) {
		key 
		insert (key, value, strlen(value));
	}
	
	for(i=0; i<2000; ++i) {
		insert (key[i], value, strlen(value));
	}
}